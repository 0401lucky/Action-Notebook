# 实现计划

- [x] 1. 添加类型定义和路由配置





  - [x] 1.1 在 `src/types/index.ts` 中添加番茄钟相关类型定义


    - 添加 `TimerState`、`TimerMode`、`PomodoroSettings`、`FocusRecord` 类型
    - 添加 `DEFAULT_POMODORO_SETTINGS` 常量
    - _Requirements: 2.1, 5.1_
  - [x] 1.2 在 `src/router/index.ts` 中添加番茄钟路由


    - 添加 `/pomodoro` 路由，指向 `PomodoroView.vue`
    - 设置 `requiresAuth: true`
    - _Requirements: 1.2_

  - [x] 1.3 在 `src/components/common/AppHeader.vue` 中添加番茄钟导航入口

    - 在 `navItems` 中添加番茄钟图标（🍅）
    - _Requirements: 1.1, 1.3_

- [x] 2. 实现番茄钟服务层





  - [x] 2.1 创建 `src/services/pomodoro.ts` 服务


    - 实现设置的本地存储读写
    - 实现专注记录的本地存储读写
    - 实现数据库同步逻辑
    - _Requirements: 5.4, 7.1, 7.2_
  - [x] 2.2 编写属性测试：设置持久化往返一致性


    - **Property 7: 设置持久化往返一致性**
    - **Validates: Requirements 5.4**

  - [x] 2.3 编写属性测试：专注记录持久化往返一致性

    - **Property 8: 专注记录持久化往返一致性**
    - **Validates: Requirements 7.1, 7.3**

- [x] 3. 实现番茄钟状态管理






  - [x] 3.1 创建 `src/stores/pomodoro.ts` Store

    - 实现计时器状态管理（idle/focusing/break/paused）
    - 实现计时器控制方法（start/pause/resume/reset）
    - 实现任务关联逻辑
    - 实现设置管理和验证
    - 实现统计数据计算
    - _Requirements: 2.1-2.5, 3.2-3.5, 4.2-4.4, 5.2-5.5, 6.1-6.3_
  - [x] 3.2 编写属性测试：计时器状态转换一致性


    - **Property 1: 计时器状态转换一致性**
    - **Validates: Requirements 2.1, 2.2, 2.3, 2.4**
  - [x] 3.3 编写属性测试：设置值验证

    - **Property 6: 设置值验证**
    - **Validates: Requirements 5.2, 5.3, 5.5**
  - [x] 3.4 编写属性测试：专注完成后模式切换

    - **Property 3: 专注完成后模式切换**
    - **Validates: Requirements 3.2, 3.3, 3.4**
  - [x] 3.5 编写属性测试：休息完成后恢复专注

    - **Property 4: 休息完成后恢复专注**
    - **Validates: Requirements 3.5**

- [x] 4. 检查点 - 确保所有测试通过





  - 确保所有测试通过，如有问题请询问用户。

- [x] 5. 实现计时器显示组件





  - [x] 5.1 创建 `src/components/pomodoro/TimerDisplay.vue`


    - 实现圆形进度指示器（SVG）
    - 显示剩余时间（MM:SS 格式）
    - 根据模式切换颜色（专注/休息）
    - _Requirements: 8.1, 8.2_

  - [x] 5.2 创建 `src/components/pomodoro/TimerControls.vue`

    - 实现开始/暂停/继续按钮
    - 实现重置按钮
    - 根据状态显示不同按钮
    - _Requirements: 2.1-2.4_

- [x] 6. 实现任务选择和设置组件





  - [x] 6.1 创建 `src/components/pomodoro/TaskSelector.vue`


    - 显示今日未完成任务列表
    - 支持选择/取消选择任务
    - 显示当前关联的任务名称
    - _Requirements: 4.1, 4.2, 4.3_
  - [x] 6.2 创建 `src/components/pomodoro/PomodoroSettings.vue`


    - 实现专注时长输入（1-60 分钟）
    - 实现短休息时长输入（1-30 分钟）
    - 实现长休息时长输入（1-30 分钟）
    - 实现输入验证和错误提示
    - _Requirements: 5.1-5.5_
  - [x] 6.3 创建 `src/components/pomodoro/PomodoroStats.vue`



    - 显示今日完成的专注时段数量
    - 显示今日累计专注时长
    - _Requirements: 6.1, 6.2_

- [x] 7. 实现番茄钟主页面





  - [x] 7.1 创建 `src/views/PomodoroView.vue`


    - 整合所有子组件
    - 实现计时器逻辑（setInterval）
    - 实现专注完成时的提示音和通知
    - 实现响应式布局
    - _Requirements: 3.1, 8.3_

  - [x] 7.2 编写属性测试：任务关联记录一致性

    - **Property 5: 任务关联记录一致性**
    - **Validates: Requirements 4.2, 4.4**

  - [x] 7.3 编写属性测试：统计数据实时更新

    - **Property 9: 统计数据实时更新**
    - **Validates: Requirements 6.3**

- [x] 8. 创建数据库迁移





  - [x] 8.1 创建 `supabase/migrations/20241202_create_focus_records.sql`


    - 创建 focus_records 表
    - 启用 RLS 并创建策略
    - 创建索引
    - _Requirements: 7.2_

- [x] 9. 最终检查点 - 确保所有测试通过





  - 确保所有测试通过，如有问题请询问用户。
